<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Matches any shared content at all (not just a URL/text
			     attachment) — deliberately broad rather than a UTI-scoped
			     dictionary predicate, so the extension is guaranteed to
			     actually appear in the Google Maps share sheet regardless
			     of exactly how Maps packages a shared place. Content this
			     app can't use (no URL/text/title found) is a no-op — see
			     ShareViewController.handleSharedItem. -->
			<key>NSExtensionActivationRule</key>
			<string>TRUEPREDICATE</string>
		</dict>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
	</dict>
</dict>
</plist>
