# Xcode
build/
DerivedData/
*.xcworkspace

# Peragra.xcodeproj itself IS committed (no XcodeGen) — only ignore the
# per-user cruft Xcode writes inside it.
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
